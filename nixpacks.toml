[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build:client", "npx tsc -p tsconfig.server.json"]

[start]
cmd = "node dist/server/index.js"